{
  "name": "browser-transformers-poc",
  "version": "1.0.0",
  "description": "Browser-based AI inference PoC using Vite, vanilla JavaScript, and transformers.js on GitHub Pages.",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage --coverage.reporter=json-summary && coverage-badges -o coverage -s coverage/coverage-summary.json --style flat && mv coverage/coverage.svg coverage/badges.svg",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "vite": "^8.1.5"
  },
  "private": true,
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "coverage-badges": "^1.0.7",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.5",
    "@playwright/test": "^1.61.1"
  }
}
