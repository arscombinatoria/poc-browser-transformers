{
  "name": "browser-transformers-poc",
  "version": "1.0.0",
  "description": "Browser-based AI inference PoC using Vite, vanilla JavaScript, and transformers.js on GitHub Pages.",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage --coverage.reporter=json-summary --coverage.reportsDirectory=coverage",
    "coverage:badge": "npm run coverage && coverage-badges"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "vite": "^8.0.10"
  },
  "private": true,
  "devDependencies": {
    "vitest": "^4.1.5",
    "@vitest/coverage-v8": "^4.1.5",
    "coverage-badges": "^1.0.7"
  }
}
